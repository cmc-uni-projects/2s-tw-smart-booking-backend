package com.example.smart_booking_system.enums;

public enum MembershipRank {
    BRONZE,   // Đồng (Mặc định)
    SILVER,   // Bạc (> 1000)
    GOLD,     // Vàng (> 5000)
    DIAMOND   // Kim cương (> 10000)
}